# Copy this file to `.env.local` and fill in your values.
#
# NOTE: With the current ShockCollar client-side implementation, the api key will be
# shipped to the browser as part of the rendered HTML/JS. Only use a key that is safe
# to expose publicly, or ask me and I can switch this to a server-proxy approach.

SHOCKCOLLAR_API_KEY="sk_live_your_key_here"
SHOCKCOLLAR_DASHBOARD_URL="https://www.shockcollar.dev"


