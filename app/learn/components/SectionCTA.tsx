export default function SectionCTA() {
  // Spacer section to push content above the grass at the bottom
  // Increased height to accommodate the footer overlay
  return <div className="h-[30vh] min-h-[250px] bg-white" />;
}
